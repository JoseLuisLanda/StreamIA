# @angular-builders/custom-esbuild (registers the esbuild alias-node-builtins plugin
# that resolves vits-web fs/path) only declares peer ranges up to Angular 19, but it
# builds fine on this Angular 21 project. Relax peer resolution so npm install works.
legacy-peer-deps=true
